# ==== Deep Learning core ====
# Cài torch theo đúng bản CUDA của máy bạn TRƯỚC (xem https://pytorch.org/get-started/locally/),
# ví dụ máy có CUDA 12.1:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# rồi mới cài các package còn lại trong file này.
torch
torchvision

# ==== Tách cảnh + đọc video ====
scenedetect[opencv]
opencv-python

# ==== Speech-to-Text (Whisper, đa ngôn ngữ, hỗ trợ tiếng Việt) ====
faster-whisper

# ==== CLIP (image encoder) ====
open_clip_torch

# ==== OCR (đọc chữ vi + en trong ảnh) ====
easyocr

# ==== BGE-M3 (text embedding) + bge-reranker-v2-m3 (CrossEncoder rerank) ====
FlagEmbedding

# ==== Vector search ====
# faiss-gpu qua pip khá bấp bênh (thường phải cài qua conda: `conda install -c pytorch faiss-gpu`).
# faiss-cpu vẫn đủ nhanh cho việc SEARCH (bottleneck chính nằm ở bước ENCODE bằng GPU, không phải
# bước search FAISS), nên mặc định dùng faiss-cpu cho chắc, nếu môi trường có conda + GPU rảnh thì
# đổi sang faiss-gpu để tăng tốc thêm bước build index.
faiss-cpu

# ==== Gemini API (caption + verify) ====
google-generativeai

# ==== UI ====
streamlit

# ==== BM25 sparse retrieval ====
rank-bm25

# ==== Tiện ích ====
Pillow
numpy
tqdm
python-dotenv

# ==== Language detection (local, không cần API) ====
# Dùng để phát hiện tiếng Việt vs tiếng Anh trước khi dịch query cho SigLIP2.
# Thay thế heuristic isascii() bị lỗi với tiếng Việt không dấu.
# (xem tokenizer_utils.py::detect_language và AIC2026_Deep_Audit_Report.md mục 3.9)
langid

# ==== NLLB-200 local translation fallback ====
# Dùng để dịch query Vi→En khi Gemini API lỗi hoặc mất mạng lúc thi đấu.
# transformers đã có sẵn (FlagEmbedding dependency), sentencepiece cần thêm.
# Model facebook/nllb-200-distilled-600M sẽ tự download (~2.4GB) lần đầu chạy.
sentencepiece

# ==== Vietnamese word tokenizer (TÙY CHỌN, cải thiện BM25 với từ ghép) ====
# Giúp BM25 tách đúng "thủ tướng" thành 1 token thay vì 2 token riêng.
# QUAN TRỌNG: nếu cài thì phải build lại BM25 index (xóa index/bm25_index.pkl).
# Lý do: tokenizer build-time và search-time phải GIỐNG NHAU (đã fix trong code).
# underthesea  # bỏ comment để cài: pip install underthesea
